<%= form_with(model: number_collectible_attribute, local: true) do |form| %>
  <% if number_collectible_attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(number_collectible_attribute.errors.count, "error") %> prohibited this number_collectible_attribute from being saved:</h2>

      <ul>
        <% number_collectible_attribute.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: timespan, local: true) do |form| %>
  <% if timespan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(timespan.errors.count, "error") %> prohibited this timespan from being saved:</h2>

      <ul>
        <% timespan.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start %>
    <%= form.time_select :start %>
  </div>

  <div class="field">
    <%= form.label :end %>
    <%= form.time_select :end %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

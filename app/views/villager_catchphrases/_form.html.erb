<%= form_with(model: villager_catchphrase, local: true) do |form| %>
  <% if villager_catchphrase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(villager_catchphrase.errors.count, "error") %> prohibited this villager_catchphrase from being saved:</h2>

      <ul>
        <% villager_catchphrase.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

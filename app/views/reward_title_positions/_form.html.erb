<%= form_with(model: reward_title_position, local: true) do |form| %>
  <% if reward_title_position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reward_title_position.errors.count, "error") %> prohibited this reward_title_position from being saved:</h2>

      <ul>
        <% reward_title_position.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

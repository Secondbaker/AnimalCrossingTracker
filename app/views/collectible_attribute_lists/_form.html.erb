<%= form_with(model: collectible_attribute_list, local: true) do |form| %>
  <% if collectible_attribute_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(collectible_attribute_list.errors.count, "error") %> prohibited this collectible_attribute_list from being saved:</h2>

      <ul>
        <% collectible_attribute_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

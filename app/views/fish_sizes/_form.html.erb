<%= form_with(model: fish_size, local: true) do |form| %>
  <% if fish_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fish_size.errors.count, "error") %> prohibited this fish_size from being saved:</h2>

      <ul>
        <% fish_size.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

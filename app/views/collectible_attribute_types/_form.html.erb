<%= form_with(model: collectible_attribute_type, local: true) do |form| %>
  <% if collectible_attribute_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(collectible_attribute_type.errors.count, "error") %> prohibited this collectible_attribute_type from being saved:</h2>

      <ul>
        <% collectible_attribute_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

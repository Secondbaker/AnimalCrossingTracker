<%= form_with(model: collectible_attribute_list_item, local: true) do |form| %>
  <% if collectible_attribute_list_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(collectible_attribute_list_item.errors.count, "error") %> prohibited this collectible_attribute_list_item from being saved:</h2>

      <ul>
        <% collectible_attribute_list_item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :collectible_attribute_list_id %>
    <%= form.text_field :collectible_attribute_list_id %>
  </div>

  <div class="field">
    <%= form.label :position %>
    <%= form.number_field :position %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: villager_gender, local: true) do |form| %>
  <% if villager_gender.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(villager_gender.errors.count, "error") %> prohibited this villager_gender from being saved:</h2>

      <ul>
        <% villager_gender.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
